import random

def generate_maze(level):
    if(level == "easy"): 
        rows, cols = 50,50
    elif (level == "medium"):
        rows, cols = 70,70
    else:
        rows, cols = 90,90

    maze = 
